# -*- mode: yaml -*-

manifest:
  version: 1.0

automations:
  create_gpt_description:
    if:
      - true
    run:
      - action: add-comment@v1
        args:
          comment: |
            Title: {{ pr.title }}
            Description: {{ pr.description }}
            Comments: {{ pr.comments | map(attr="content") }}
