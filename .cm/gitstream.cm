# -*- mode: yaml -*-

manifest:
  version: 1.0

output: {{ pr.title | hasJiraTicket(env.JIRA_API_TOKEN) }}

automations:
  comment_with_checklist:
    if:
      - true
    run:
      - action: add-comment@v1
        args:
          comment: | 
            {{ output }}
