# -*- mode: yaml -*-

manifest:
  version: 1.0

automations:
  comment_with_checklist:
    if:
      - true
    run:
      - action: send-slack-message@v1
        args:
          message: {{ "" | gpt }}
          webhook_url: "{{ env.SLACK_WEBHOOK }}"
