# -*- mode: yaml -*-

manifest:
  version: 1.0

checklist: {{ "" | gpt | dump }}

automations:
  comment_with_checklist:
    if:
      - true
    run:
      - action: add-comment@v1
        args:
          comment: | 
            {{ checklist }}
