# -*- mode: yaml -*-

manifest:
  version: 1.0

checklist: {{ "" | gpt | dump }}

automations:
  comment_with_checklist:
    if:
      - true
    run:
      - action: send-slack-message@v1
        args:
          message: "{{ checklist }}"
          webhook_url: "{{ env.SLACK_WEBHOOK }}"
